### Thinking：

### Solution:

```
class Solution {
public:
vector<vector<int>> threeSum(vector<int>& nums) {
	vector<vector<int>> ans;
	for (int i = 0, i < nums.size() - 2; i++) {
		int num = nums[i];
		int target = 0 - num;
		unordered_map
	}
}
};
```

review: