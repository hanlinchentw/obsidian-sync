### Thinking：


### Solution:

```
class Solution {
public:
	int openLock(vector<string>& deadends, string target) {
		unordered_set<string> visited;
		string initial = "0000";
		queue<pair<instring>> que;
		que.push(initial);
		
		while (!que.empty()) {
			string front = que.front(); que.pop();
			if (front == target)
				return 
		}
		
	}
};
```

review: