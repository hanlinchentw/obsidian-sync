### Thinking：

### Solution:

```cpp
class Solution {
public:
    vector<string> removeInvalidParentheses(string s) {
        
    }

	void dfs(string s, string current, int n,vector<string>& ans) {
		if (n < 0) return;
		if (s.empty()) {
			if(n == 0) {
				ans.push_back(current);
			}
			return;
		}
		if (s[0] == '(') {
			dfs(s.substr(1), current+);
		} else {
		}
	}
};
```

review: