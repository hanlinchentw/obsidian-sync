### Thinking：

### Solution:

```cpp
class Solution {
public:
    vector<string> findWords(vector<vector<char>>& board, vector<string>& words) {
	    unordered_set<string> ans;
	    set<pair<int, int>> visited;
	    for (int i = 0; i < board.size(); i++) {
			for (int j = 0; j < board[0].size(); j++) {
				char ch = board[i][j];
				string s(1, ch);
				if (canProceed(s, words)) {
					solve(board, s, i, j, ans, words, visited);
				}
			}
		}
	    return vector<string>(ans.begin(), ans.end());
    }

	void solve(const vector<vector<char>>& board, string s, int row, int col, unordered_set<string>& ans, const vector<string>& words, set<pair<int, int>>&  visited) {
		if (find(words.begin(), words.end(), s) != words.end()) {
			ans.insert(s);
			return;
		}
		visited.insert({row, col});
		vector<pair<int, int>> dirs = {{0, 1}, {0, -1}, {1, 0}, {-1, 0}};
		for (auto dir : dirs) {
			int i = row + dir.0;
			int j = col + dir.1;
			if (visited.find({i, j}) != visited.end()) continue;
			char ch = board[i][j];
			s.push_back(ch);
			if (canProceed(s, words)) {
				solve(board, s, i, j, ans, words, visited);
			}
			s.pop_back();
		}
	}

	bool canProceed(string s, const vector<string>& words) {
		for (string word : words) {
			if (word.starts_with(s)) return true;
		}
		return false;
	}
};
```

review: