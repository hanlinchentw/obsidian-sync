### Thinking：
1. prepare a `vector<string>` ans, unordered_set visited, and current string, index

### Solution:

```cpp
class Solution {
private:
	unordered_map<char, vector<char>> comb;
public:
    vector<string> letterCombinations(string digits) {
	    for (int i = 2; i <= 9; i++) {
			if i == 2 {
				comb['2'] = {'a', 'b', 'c'}
			} else if i == 3 {
				comb['3'] = {'d', 'e', 'f'}
			} else if i == 4 {
				comb['3'] = {'g', 'h', 'i'}
			} else if i == 5 {
				comb['3'] = {'j', 'k', 'l'}
			} else if i == 6 {
				comb['3'] = {'m', 'n', 'o'}
			} else if i == 7 {
				comb['3'] = {'p', 'q', 'r', 's'}
			} else if i == 8 {
				comb['3'] = {'t', 'u', 'v'}
			} else if i == 9 {
				comb['3'] = {'x', 'y', 'z'}
			}
	    }
	    vector<string> ans;
	    
    }

	void dfs(string digits, int index, string curr, unordered_set<string>& ans) {
		if (index == digits.length()) {
			ans.insert(curr);
		}
		
		for (int i = index; i < digits.length(); i++) {
			char digits = []
		}
	}
};
```

review: